define([
    'app'
], function (app) {
    "use strict";

    app.controller('homeController', ['$scope', '$log', function ($scope, $log) {
        "use strict";

        $log.debug("Init home controller");

    }]);
});
